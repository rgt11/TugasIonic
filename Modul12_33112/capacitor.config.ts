import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Modul12_33112',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
